export type Link = {
    /**
   * The label or name of the link (e.g. about, facebook, etc.)
   */
    name: string,
    /**
   * The destination URI/URL (e.g. https://scuzzyfox.com)
   */
    href: string

}

